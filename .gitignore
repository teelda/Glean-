node_modules/
.next/
.next-verification/
.open-next/
.pnpm-store/
dist/
output/
.openai/
.vercel/
.env*
.DS_Store
tsconfig.tsbuildinfo

# Local form backend store — contains real respondent answers.
tmp/
.vercel

# End-to-end dev servers build here, one directory each.
.next-e2e-*
test-results/
playwright-report/

# Regenerated by every `next dev`/`next build`, and the two end-to-end servers
# rewrite its reference path to whichever build directory ran last. Next.js
# generates it on demand, so tracking it only produced churn.
next-env.d.ts
